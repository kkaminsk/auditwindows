# M-4 Tasks

- [x] Update `Setup-AuditWindowsApp.ps1` default output directory from `$env:USERPROFILE` to `[Environment]::GetFolderPath('MyDocuments')`
- [x] Ensure directory creation guard exists (like main script)
- [x] Create GIVEN/WHEN/THEN spec
